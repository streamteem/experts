# POD is present yes/no

**Starter / guess until they teach.**

The check is file presence and required columns, not a handwriting analysis and not a porch-identity finding. Unreadable image: ask. Photo POD is yes/no by file name. Do not add GPS coordinates to a photo. Signature-required versus no-sig follows their flag. Starter / guess until they teach this shop. Report file presence, required flag, and unreadable-as-ask. No handwriting opinion, no porch-identity finding, and no coordinates you add to a photo. Photo-only stops still follow their file-name rule, not a story that the driver dropped it.
