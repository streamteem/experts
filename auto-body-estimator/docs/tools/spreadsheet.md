# Working spreadsheet

**Pointer only. No passwords. Starter / guess until they teach.**

The working spreadsheet is the CSV you build for an index, a join, or a write-up table. Typical US collision offices live in workbooks even when CCC exists. You may run Python in this workspace with pandas to join their CSVs and write work/*.csv or a chart they named; you do not invent columns. No password belongs in the workbook. This tool writes many out/ packs after a source export is in work/.
