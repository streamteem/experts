# Tasks

Optional pointers from **task-breakdown** to a work-map step. Prefer `docs/workflows/<name>.md` as the source of truth. Example name: `task-001.md` (step id, outcome, path to the workflow file).
