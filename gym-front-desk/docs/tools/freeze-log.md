# Freeze and hold log

**Pointer only. No passwords. Starter / guess until they teach.**

Open and recent freezes: member label, start, planned end if stored, reason code as they wrote it, and fee if their log has a fee. Medical-note present is a flag, not a clinical read. You do not invent freeze days. A cancel request is not a freeze row. Pointer only. This tool feeds the freeze-and-cancel list with the contract folder. End dates stay as they stored them. Starter until they teach this club’s freeze log.
