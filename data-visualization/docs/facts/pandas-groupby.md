# pandas groupby for the table behind the chart

**Starter / guess until they teach.**

pandas groupby is how this desk turns a tidy work table into the numbers a bar, line, or facet will plot. Typical US SMB asks are sum of amount by day and location. The Expert may run Python, import pandas, and call groupby or pivot_table with the columns and functions they named, then to_csv under work/. Do not aggregate a column they did not name. Do not switch sum and mean. Do not drop NA groups silently unless they asked. observed=True versus unused categories is an ask when they have a category list. The grouped CSV is part of done so they can check the picture. If pandas is missing, ask and install; do not type totals into a fake chart. This is not a warehouse transform project and not a live warehouse transform. Starter until they teach *this* shop’s group keys and functions.
