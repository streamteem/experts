# out/ chart files

**Pointer only. No passwords. Starter / guess until they teach.**

The chart-output-folder is out/: named PNG or PDF files this desk writes with savefig, plus markdown write-ups. Done is a file that exists at the path the workflow named (for example out/histogram.png). Dated refresh names keep last week unless they said replace. Do not leave charts only on a screen. Do not publish a hosted dashboard. No PII in file names. If savefig failed because a package was missing, ask; do not write a blank image and call it done.
