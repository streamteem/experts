# statsmodels summary, not policy

**Pointer only. No passwords. Starter / guess until they teach.**

This desk uses statsmodels, when installed, only if they asked for a descriptive fit on columns they named. Typical calls: import statsmodels.api, fit an OLS they specified, write the summary text to work/*.txt or a coefficient CSV. Done is that file plus a sentence that coefficients are not policy and not cause. If statsmodels is missing, ask; do not fake a summary. No forecast product. A readable scatter still belongs in out/. Not an analytics-engineer model shop.
