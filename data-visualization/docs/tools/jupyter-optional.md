# Jupyter notebook (optional)

**Pointer only. No passwords. Starter / guess until they teach.**

A Jupyter notebook is optional scratch, not required for done. If they use one, the Expert still writes the PNG and work CSV to disk so a check can see files. Typical use is to inspect dtypes, then copy the working cells into a .py they can rerun. Do not keep secrets in the notebook. Do not treat an unsaved kernel as the pack. If Jupyter is missing, skip it; Python scripts are enough. This is not a teaching seminar product.
