# Spreadsheet or workbook they dropped

**Pointer only. No passwords. Starter / guess until they teach.**

The spreadsheet tool is an Excel or similar workbook they saved: a wide month grid or a printed-looking sheet. The Expert may run Python, import pandas, and call read_excel on the sheet they named, or they export a CSV first. Done is a work CSV with real headers, not the title row. Do not invent sheets. Merged cells and extra header rows are asks. No workbook password stored here. If the engine for xlsx is missing, ask to install; do not fake a read.
