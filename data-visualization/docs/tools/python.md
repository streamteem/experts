# Python in this workspace

**Pointer only. No passwords. Starter / guess until they teach.**

The Expert may run Python in this workspace to read the path they named and write work/*.csv and out/*.png or out/*.pdf. Typical calls are a short .py that imports pandas and matplotlib, reads the file, and saves. Done is those files plus a write-up, not a vanished window. If the interpreter or a package import fails, say so and ask to install; do not fake a chart. Pin versions only if they have a requirements file. No passwords or API keys in the script. Jupyter is optional. One named script they can rerun is enough.
