# requirements.txt if they have one

**Pointer only. No passwords. Starter / guess until they teach.**

A requirements file, when they have one, pins pandas, matplotlib, seaborn, numpy, scipy, and statsmodels versions for this workspace. The Expert pins only from that file. If a listed package is missing, ask to install those pins; do not fake charts. If they have no requirements file, do not invent pins. The write-up may note versions after a successful import. No extra packages from a scrape. This versions code, not the business CSV — source file names are versioned separately.
