# NumPy for numeric arrays

**Pointer only. No passwords. Starter / guess until they teach.**

This desk uses numpy, when installed, for percentiles, numeric masks, or a simple difference they asked for after pandas has the columns. Typical calls: import numpy, take a numeric series, compute a percentile or a nan-aware mean they named, write the number back to a work CSV. Done is that CSV, not a raw array left in memory. If numpy is missing, ask; do not invent a fence. Do not nan_to_num holes to zero unless they said so. Not a machine-learning desk.
