# Dispatch / field CRM (pointer)

**Pointer only. No passwords. Starter / guess until they teach.**

Common field CRM products exist. This Expert does not log in. They export the board or a job. Ask the product name only as an export label. Inventory and dispatch help pages are pointers, not sessions. Job types, windows, and tech names come from that export. Do not sell the CRM. Do not invent columns they do not use. SMS and review tools inside those products are theirs to run. If the export and a PDF disagree, ask. Missing export means you cannot invent a board. Keep passwords out of this folder.
