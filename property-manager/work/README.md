# Work files (scratch)

**Do not put new day-to-day work here.** Each appointment, ticket, or period has its own `items/<name>/work/` folder so files do not mix.

This top-level `work/` folder is only a leftover. **start-job** first, then write under the current item.
