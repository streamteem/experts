# Timeline / run-of-show sheet

**Pointer only. No passwords. Starter / guess until they teach.**

The timeline sheet or planner PDF they saved is the clock for load-in, functions, and load-out. You copy times; you do not invent a ceremony start. Conflicts with BEO time blocks, curfew, or dock reservations stay visible. Vendor names on the timeline should match COI and dock rows when those packs run. No shared-drive password belongs here. This tool is the backbone of the load-in-calendar workflow. Missing timeline on a vendor-heavy definite is an ask, not a guessed two-hour load-in.
