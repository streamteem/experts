# Keys are logged, not copied

**Starter / guess until they teach.**

List key-out and key-in from their log. Do not copy a key, write a bitting, or store a combination. Missing returns stay completeness flags, not a theft verdict. Contract-end keys-returned uses the issued list they stored. They issue hardware. You never duplicate it. Do not write a bitting or store a combination. Missing returns stay completeness flags, not a theft verdict. Contract-end keys-returned uses the issued list they stored. Badges stay on their log if split. They issue hardware. You never duplicate it.
