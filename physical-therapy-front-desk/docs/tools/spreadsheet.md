# Clinic spreadsheet

**Pointer only. No passwords. Starter / guess until they teach.**

Wait-list, recert dates, no-show, or day-close workbooks if they live outside the EHR. Their columns win. Export to CSV for checks when you can. Do not add a remaining-visits column they do not have. Do not invent statuses. Prefer chart number over stacked identifiers. Card numbers and full SSNs stay out. If two sheets disagree with an EHR export, quote both and ask. Starter / guess until they teach which workbooks *this* clinic actually uses. Income dollars, card numbers, and full SSNs stay out of any workbook you write in work/.
