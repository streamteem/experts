# Local hourly vs weight

**Starter / guess until they teach.**

Billing method is hourly local versus weight-or-cube as they coded it on the job. Typical US SMB shops use hours plus extras inside a city radius they already wrote, and a weight path on longer jobs. Copy the method. Do not switch a job to weight because you want a pound, and do not switch a weight job to hourly to skip a missing scale ticket. Hours come from their ticket; weight comes from scale or file; never invent either. Rate lines still come from the matching sheet in their rate file. This Expert keeps the method they stored and flags a mismatch between method and missing ticket. They choose the product. Starter until they teach *this* shop’s radius and which extras apply to each method. You do not give a tariff opinion about which method is required. Fuel surcharge still comes only from their file.
