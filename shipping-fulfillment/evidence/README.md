# Evidence

Logs from **tool-operator** and **run-until-check**. Name them `<date>-<step-or-tool>.log` and `<task>-verify.log`.

A check command’s stdout/stderr goes here. Exit code 0 means the step passed. Anything else is a fail; **run-until-check** reads this file before it retries.
